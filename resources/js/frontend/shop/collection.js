import Alpine from './alpine-init.js';

// La página de colección no tiene componentes propios; el entry arranca
// Alpine (header, buscador, carrito, acordeón FAQ, carruseles de secciones).
Alpine.start();
